<div class="card">
  <% link_to movie_path(movie) do %>
    <%= image_tag movie.poster_url, class: "card_img_top" %>
    <div class="card-body">
      <h5 class= "card-title"><%= movie.title %></h5>
    </div>
  <% end %>
</div>
